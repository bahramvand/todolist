# todolist

1. docker compose up -d db

2. docker compose down

3. poetry install

4. poetry run alembic upgrade head

5. poetry run todolist

6. poetry run todolist tasks:autoclose-overdue

7. poetry run todolist tasks:start-autoclose-scheduler
